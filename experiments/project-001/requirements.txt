# Host-side tooling (runs on your laptop, NOT on the ESP32)
esptool>=4.7      # erase/flash the MicroPython firmware image onto ESP32
mpremote>=1.22     # copy files to the board, open REPL, run scripts
